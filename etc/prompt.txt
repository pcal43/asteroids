Here’s the updated single block with those changes folded in:

This project contains a skeleton for a PICO-8 game. It includes a top-level controller, a title screen, and a game screen. The project establishes conventions for object-oriented-style Lua using coarse-grained objects such as screens with `update()` and `draw()` methods.

Please replace both `GameScreen.lua` and `TitleScreen.lua` behavior as follows. The game should no longer use a separate title screen flow. Instead, the initial `GameScreen.lua` should begin in an attract-mode title state showing the first wave already active in the background. In this state, six large asteroids should already be floating and moving on screen according to the normal asteroid movement rules. Superimposed over them, draw the game title `AISTEROIDS` in large block letters using only the `line()` function. Also display a flashing `press x to start` prompt. While in this attract-mode title state, the asteroids should continue moving normally in the background, but gameplay should not yet begin and the player ship should not yet be active. When the player presses X, the title text and prompt should disappear and the game should transition directly into active play using the same asteroids that were already floating on screen. Do not discard and respawn the initial asteroid field when the player starts the game.

The game should be inspired by Atari Asteroids and use purely geometric rendering. All graphics must be drawn using `line()` and `circ()`/`circfill()`. Do not use sprites.

All gameplay logic should live in `GameScreen.lua`. Follow the object-oriented conventions already present. Define objects such as `Ship`, `Asteroid`, `Bullet`, and `UFO` as Lua tables with methods. Each object should implement `update(dt)` and `draw()`. Maintain collections for asteroids, bullets, enemy bullets, and UFOs. Use a consistent delta time system in seconds for all movement and timing.

The player controls a spaceship represented by an isosceles triangle. This requirement is important: the ship must be visibly triangular, not a narrow wedge or nearly straight line. The triangle’s nose angle must be 45 degrees total, meaning 22.5 degrees on each side of the forward axis. The altitude of the triangle, measured from the nose to the midpoint of the base, should be 8 pixels. Compute the base width from that altitude and nose angle so the shape is mathematically correct. Draw the ship in white using `line()`.

When gameplay begins, the ship should start at the center of the screen `(64,64)`, facing upward, with zero velocity.

Controls: pressing Right rotates the ship clockwise, and pressing Left rotates the ship counterclockwise. This mapping is important and must not be reversed. Rotation should continue while the button is held, at 180 degrees per second. Pressing the X button applies thrust in the ship’s facing direction. Use thrust acceleration of 20 pixels per second squared. When not thrusting, apply velocity damping at 1 pixel per second squared until velocity reaches zero. Cap the maximum ship velocity at 20 pixels per second. While thrusting, draw a red equilateral triangle behind the ship, pointing opposite the ship’s facing direction. When thrust is active, play sound 4.

Pressing the O button fires a bullet from the ship’s nose in the current facing direction. Bullets travel at 20 pixels per second and disappear after traveling 64 pixels. Bullets are 2 pixels in size and orange in color. Add a small firing cooldown of about 0.2 seconds. Whenever the player fires a projectile, play sound 2.

For the initial wave, spawn 6 large asteroids at random positions, ensuring none are within 20 pixels of the eventual player start position at the center of the screen. Asteroids move in random directions at speeds between 5 and 10 pixels per second. Each subsequent wave should contain one more starting large asteroid than the previous wave, up to a maximum of 10 large asteroids per wave.

There are three asteroid sizes: large with 16 pixel diameter, medium with 12 pixel diameter, and small with 8 pixel diameter. Asteroids are drawn as outlined circles using `circ()`. When a large asteroid is hit, it splits into two medium asteroids. When a medium asteroid is hit, it splits into two small asteroids. Small asteroids are destroyed when hit. New asteroids inherit the parent position and move in random directions. Whenever any asteroid is hit by any projectile, play sound 3.

Use simple circular collision detection for all interactions. Player bullets destroy asteroids. The player ship is destroyed on collision with an asteroid. Player bullets destroy UFOs. UFO bullets can destroy asteroids but do not award points. The UFO can also collide directly with an asteroid; if this happens, both the UFO and the asteroid are destroyed.

When the player’s ship is destroyed, do not immediately respawn it. Instead, play a short destruction animation at the ship’s position, then remove the ship from play entirely for a short delay of at least 3 seconds before spawning the next life. During this time the player cannot move or fire. When placing the next ship, care must be taken to ensure the spawn location is not within 15 pixels of any asteroid. If the center of the screen is unsafe, wait until a suitable spawn location becomes available rather than spawning the ship into danger. When the new ship is finally placed, reset its velocity to zero and grant temporary invulnerability of about 2 seconds.

The game world wraps on all edges with toroidal topology. If an object moves past one edge of the 128 by 128 screen, it should appear on the opposite side. This applies to the ship, asteroids, bullets, and UFOs, except that the UFO should not wrap during its pass. The UFO should make a single pass from left to right or right to left across the screen, and when it reaches the far side it should disappear rather than wrapping.

The player starts with 3 lives. Display remaining lives as small upward-pointing isosceles triangles in the upper-left corner. These life icons should match the player ship shape and proportions, scaled down appropriately.

The player earns points: 10 for large asteroids, 20 for medium asteroids, 30 for small asteroids, and 100 for destroying a UFO. Display the score in the upper-right corner.

When all asteroids are destroyed, wait 3 seconds, then spawn the next wave following the wave-count rules above: start at 6 large asteroids, then increase by 1 per wave until reaching a maximum of 10.

At random intervals between 30 and 60 seconds, spawn a UFO. It travels horizontally across the screen from left to right or right to left at about 15 pixels per second, making a single pass and disappearing when it exits the far side of the screen. The UFO should be drawn as a green horizontal rectangle, roughly comparable in overall size to the player’s ship. Care must be taken to ensure that the UFO is not spawned within 20 pixels of any asteroid or the player ship. If a valid spawn location is not currently available, delay UFO entry until one is available. When the UFO enters the screen, begin continuously playing sound 5 in a loop for as long as the UFO is active; stop that looping sound when the UFO is destroyed or leaves the screen. The UFO periodically fires 2 pixel green bullets in random directions, about once every 1 to 2 seconds. All UFO bullets should move at 10 pixels per second. Whenever the UFO fires a projectile, play sound 2. UFO bullets can destroy both the player and asteroids, but asteroid destruction by UFO bullets does not award points. If the player’s bullet hits the UFO, it is destroyed and awards 100 points. If the UFO collides with the player, both are destroyed.

In the background, alternate between sound 0 and sound 1 in a slow, repeating two-beat cadence reminiscent of the movie Jaws. This background cadence should begin at one beat every 2 seconds when a wave starts with its full asteroid count present. As the number of asteroids on the field decreases, the cadence should steadily accelerate. By the time only a single asteroid remains, the cadence should have increased to 2 beats per second. Implement this as a smooth progression based on the number of asteroids currently remaining, and keep alternating between sound 0 and sound 1 throughout gameplay except where game-state transitions make silence more appropriate.

All required sound effects (sound indices 0 through 5) have already been created and are available. Do not generate or define new sound effects; only trigger the existing ones at the appropriate times described above.

Ensure clean separation of update logic and rendering logic. The final implementation should be complete, readable, and idiomatic PICO-8 Lua, with no placeholder code.
